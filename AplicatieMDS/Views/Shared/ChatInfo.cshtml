@model AplicatieMDS.Models.Chat

<div class="card-body">
    <div class="d-flex justify-content-between flex-row mt-5">
        <div><i class="bi bi-person"></i> @Model.CurrentUser.UserName</div>
        <div><i class="bi bi-person"></i> @Model.FriendUser.UserName</div>
    </div>
</div>
